[project]
name = "rag-facile-tools"
version = "0.1.0"
description = "Internal tooling for RAG Facile template generation"
requires-python = ">=3.13"
dependencies = [
    "libcst>=1.8.6",
    "pyyaml>=6.0.2",
    "rich>=13.0.0",
]

[tool.uv]
package = false
