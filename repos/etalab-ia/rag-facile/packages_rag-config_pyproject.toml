[project]
name = "rag-config"
version = "0.8.0" # x-release-please-version
description = "Configuration management for RAG Facile - TOML-based settings with validation and presets"
readme = "README.md"
requires-python = ">=3.13"
license = { text = "MIT" }
authors = [{ name = "Etalab", email = "etalab@modernisation.gouv.fr" }]
keywords = ["rag", "config", "configuration", "toml", "validation"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
]
dependencies = [
    "pydantic>=2.0.0",
    "tomli-w>=1.0.0",
]

[project.urls]
Homepage = "https://github.com/etalab-ia/rag-facile"
Documentation = "https://github.com/etalab-ia/rag-facile/blob/main/docs/config-guide.md"
Repository = "https://github.com/etalab-ia/rag-facile"
Issues = "https://github.com/etalab-ia/rag-facile/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rag_config"]

[tool.hatch.build.targets.wheel.force-include]
"presets" = "rag_config/presets"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
