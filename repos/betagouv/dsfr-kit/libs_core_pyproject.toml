[project]
name = "dsfr-kit-core"
version = "0.1.0"
description = "Future Python implementation for DSFR component library (Streamlit, Reflex)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dsfr_kit_python_core"]
