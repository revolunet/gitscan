{
  "name": "@mutafriches/shared-types",
  "description": "Types partagés entre l'API et l'UI de Mutafriches",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "excel-to-fixture:all": "tsx src/test-cases/scripts/excel-to-fixture.ts --all",
    "excel-to-fixture:all:format": "tsx src/test-cases/scripts/excel-to-fixture.ts --all && prettier --write src/test-cases/fixtures/**/*.json"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@vitest/ui": "^4.0.8",
    "tsx": "^4.20.5",
    "typescript": "^5.3.3",
    "vitest": "^4.0.14",
    "xlsx": "^0.18.5"
  }
}
