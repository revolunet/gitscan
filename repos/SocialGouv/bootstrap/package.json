{
  "private": true,
  "description": "The Social Gouv Bootstrap monorepo",
  "scripts": {
    "serve": "lerna run serve --parallel",
    "build": "lerna run build",
    "lint": "lerna run lint --parallel",
    "test": "lerna run test --parallel"
  },
  "workspaces": [
    "docs",
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^9.0.0"
  }
}
